UI - User Interface
ReactJS - JavaScript Library
Component based architecture
- Component - Small reusable piece of UI

Fast and scalable front-end development
- Developed by Facebook
- Single Page Application (SPA) support
- Virtual DOM for performance
- updates only changed parts of the UI

Before ReactJS
- Manual DOM manipulation
- State management challenges (Hard to maintain, debug, and test)
- Tight coupling of UI and logic


Facebook Created ReactJS 
 For,
    - Building user interfaces
    - Loosely coupled components
ReactJS is a JavaScript library for building user interfaces, particularly single-page applications where you need a fast and interactive user experience. It allows developers to create large web applications that can change data, without reloading the page. Its key features include:
- Component-Based Architecture: React allows developers to build encapsulated components that manage their own state, then compose them to make complex UIs. This promotes reusability and separation of concerns.
- Virtual DOM: React uses a virtual representation of the DOM to optimize updates. When the state of an object changes, React first changes the object in the virtual DOM, then it compares the virtual DOM with the real DOM and updates only the parts that have changed, which improves performance.
- Unidirectional Data Flow: Data flows in one direction, making it easier to understand how data changes affect the UI. This helps in debugging and maintaining the application.
- JSX: React uses JSX, a syntax extension that allows mixing HTML with JavaScript. This makes it easier to write and visualize the structure of the UI.
 - JavaScript XML (JSX): JSX is a syntax extension for JavaScript that allows you to write HTML-like code within your JavaScript files. It makes the code more readable and easier to write, as it closely resembles the structure of the UI.
 